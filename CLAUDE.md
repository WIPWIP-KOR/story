# 저장소 규칙

이 저장소는 어린이 애니메이션의 기획 문서(세계관·캐릭터·시나리오)를 관리합니다.
코드가 아니라 **문서가 결과물**입니다.

## 일관성 규칙

- **이 작품은 한국어 베이스다.** 모든 문서, 대사, 노래, 이름, 언어유희는
  한국어로 먼저 만든다. 캐릭터·에피소드 이름에 영어를 쓰지 않는다.
- 모든 창작 판단(어휘 수준, 유머, 소재)은 `bible.md`의 대상 연령과 톤을 기준으로 한다.
  `bible.md`와 충돌하는 내용을 쓰지 말고, 충돌이 필요하면 `bible.md` 수정을 먼저 제안한다.
- 캐릭터 1명 = `characters/` 아래 파일 1개. 에피소드 1편 = `scenarios/` 아래 파일 1개.
- 새 문서는 반드시 같은 폴더의 `_template.md` 구조를 그대로 따른다. 섹션을 임의로 빼지 않는다.
- 캐릭터를 추가·삭제하면 `characters/README.md` 인덱스를, 에피소드를 추가·삭제하면
  `scenarios/README.md` 인덱스를 같은 커밋에서 갱신한다.
- 시나리오에 새 인물이 등장하면 먼저 `characters/`에 캐릭터 파일부터 만든다.
  캐릭터 파일 없는 인물을 시나리오에 등장시키지 않는다(단역 군중 제외).

## 파일 이름

- 캐릭터: `characters/<이름>.md` — 한글 이름 그대로 사용 (예: `characters/또리.md`)
- 에피소드: `scenarios/ep<2자리 번호>-<제목>.md` (예: `scenarios/ep01-잃어버린-모자.md`)
- `_` 로 시작하는 파일은 템플릿이므로 내용을 채우지 말고 복사해서 사용한다.

## 문서 상태

- 모든 캐릭터·에피소드 문서 머리말의 `상태` 필드는 `초안` / `검토중` / `확정` 중 하나.
- `확정` 문서의 설정(이름, 성격, 외형, 관계)을 바꿀 때는 그 설정을 참조하는
  다른 문서를 검색해서 함께 고친다.

## 어린이 콘텐츠 원칙

- 폭력, 공포, 차별적 표현을 쓰지 않는다. 갈등은 오해·실수·경쟁 수준으로 유지하고
  에피소드 안에서 따뜻하게 해소한다.
- 캐릭터의 외형 묘사는 클로드 디자인의 이미지 생성 프롬프트 재료가 되므로,
  색상·체형·의상·소품을 추상어 없이 구체적으로 쓴다.
